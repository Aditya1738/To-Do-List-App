# React + Vite

# React To‑Do List App

A simple and responsive to‑do list application built with React.js.

## Features
- Add new tasks using an input field and button  
- Mark tasks as completed using a checkbox  
- Delete tasks from the list  
- Clean, minimal UI with separate components (`InputContainer`, `TodoContainer`, `Todo`)

## Tech Stack
- React.js
- JavaScript (ES6+)
- CSS

## Getting Started
1. Clone the repository  
2. Run `npm install`  
3. Run `npm start` to start the development server  

## What I Learned
- Managing state and props in React components  
- Handling form input and events  
- Structuring a small project into reusable components  
